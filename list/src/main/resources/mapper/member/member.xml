<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.example.pro.Mapper.listMapper">
    
    <select id="list" resultType="com.example.pro.Vo.listVo">

    <![CDATA[
    	select * from(select * from (select ROWNUM r,b.* from board b where btitle like '%'||#{searchword}||'%' or bname like '%'||#{searchword}||'%') where r>=#{startrow} and r<#{endrow})
    ]]>
    </select>
    
    <select id="listCount" resultType="int">
    	select count(*) from board where btitle like '%'||#{searchword}||'%' or bname like '%'||#{searchword}||'%'
    </select>
    
    <select id="listserachCount" resultType="int">
	
	<![CDATA[
		select count(*) from board where btitle like '%'||#{searchword}||'%' or bname like '%'||#{searchword}||'%'
 	]]>
    </select>
    
     <select id="listserach" resultType="com.example.pro.Vo.listVo">

    <![CDATA[
    	select * from(select * from (select ROWNUM r,b.* from board b where btitle like '%'||#{searchword}||'%' or bname like '%'||#{searchword}||'%') where r>=#{startrow} and r<#{endrow})
    ]]>
    </select>
    
    
    </mapper>